#include <stdio.h>

int main(void)
{
    
    // Unsigned char byte is the size of a byte?
    unsigned char byte;
    printf("The size of a byte: %d\n", sizeof(byte));
    printf("Calculating machine epsilon: ")
    return 0;
}
